schema: mcpg.dev/plugin/v1
id: dev.mcpg.call-logger
name: Call Logger
license: Apache-2.0
description: |
  Structured tracing output for every tool call. Never blocks
  requests; emits pre-dispatch and post-dispatch log entries with
  configurable sampling and field redaction.
class: tool_gate
runtime: native-cdylib-v1
protocol_version: "1.0"
required_capabilities: []
